node_modules
dist
*.tsbuildinfo

# Build-time data — regenerated by scripts/prebuild.mjs (npm run prebuild:data).
# Derived from /packs, never edited by hand, so it is not committed.
src/generated/
public/manifest.json
public/packs/
