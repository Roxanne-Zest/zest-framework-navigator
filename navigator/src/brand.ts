// Brand — reused from the Zest Delivery shell (src/brand.ts) so the navigator
// reads as a module of the same product. The citrus design tokens themselves
// live in styles/tokens.css, ported verbatim from the shell's dashboard.css.
export const BRAND = {
  name: "Zest Delivery",
  product: "Framework Navigator",
  tagline: "Path to Service",
  logo: "./logo.svg",
};
