# `/packs/tools` — tool packs

One folder per tool, named with the tool slug (e.g. `maturity-assessment/`,
`relationship-weighting/`). Same six-part pack shape as elements and outcomes.

Empty for now. See [`/_schema/pack.frontmatter.md`](../../_schema/pack.frontmatter.md) for
the pack shape.
